/// <reference types="jasmine" />

describe('The Demo Testing', () => {
  it('should be truthy', () => {
    expect(true).toBeTruthy();
  });

  it('should be false', () => {
    expect(false).toBeFalsy();
  });
});

// it('should throw error', () => {
//   throw new Error('Something went wrong');
// });
